Number default_tnl_warn_timer_value (gPersist_On_Every_Change)

Switch sw_init_default_tnl_warn_timer_setpoint // init all tunnel values to defaults. Init defaults in DB on the first run Then comment the lines in tnl.rules rule! Note: Tunnels should not be enabled when using
